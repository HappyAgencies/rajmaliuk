import { Allura } from 'next/font/google';
import { Montserrat } from 'next/font/google';
 
export const allura = Allura({ subsets: ['latin'], weight:['400'], });
export const Mont = Montserrat({ subsets:['latin'], weight:['500'],});